# Laptop Setup — Later

When the laptop arrives:

1. Install Flutter SDK and Android tooling.
2. Create/open this project.
3. Run `flutter pub get`.
4. Connect an Android device or emulator.
5. Run `flutter run`.
6. Test the foundation navigation.
7. Add backend credentials only through the approved secure configuration process.

Do not paste production secrets into chat or into source code.
